{
	"name": "starktma",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"build": "npm run build --workspace=@starktma/property-database && npm run build --workspace=@starktma/minecraft-utils && npm run build --workspace=@starktma/inventory-manager && npm run build --workspace=@starktma/game-state-machine",
		"package": "npm run package --workspaces --if-present",
		"publish": "npm publish --workspaces --access public"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"type": "module",
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"publishConfig": {
		"access": "public"
	}
}
