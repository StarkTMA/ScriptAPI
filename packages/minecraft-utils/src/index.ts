// Re-export configuration functions for convenience
export { setNamespace, getNamespace } from "./config";

// Re-export constants with the new dynamic namespace
export { getPackageNamespace } from "./constants";
